<?php
echo "<pre>";

$a = 0;
$b = 1;
$soma = $a + $b;
var_dump($soma);
echo($soma);
$subtracao = $a -$b;
var_dump($subtracao);
echo($soma);
$multiplicacao = $a * $b;
var_dump($multiplicacao);
echo($soma);
$divisao = $a / $b;
var_dump($divisao);
echo($soma);
$modulo = $a % $b;
var_dump($modulo);
echo($soma);


/* Operadores Aritiméticos

    Crie duas variáveis a e b, atribua valores numéricos a elas;
    Crie uma variável com o valor da soma das variáveis anteriores;
    Crie uma variável com o valor da subtração das variáveis anteriores;
    Crie uma variável com o valor da divisão das variáveis anteriores;
    Crie uma variável com o valor da multiplicação das variáveis anteriores;
    Crie uma variável com o valor da módulo(resto da divisão) das variáveis anteriores;
    Exiba os valores das variáveis;
*/